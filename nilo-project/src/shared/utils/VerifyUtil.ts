/**
 * 密码正则：8–20 位，须含字母和数字，可选符号 !@#$%^&*()_+-=
 * 邮箱验证码：6 位数字+字母（大小写均可）
 */
export const regs = {
    password: /^(?=.*[A-Za-z])(?=.*\d)[A-Za-z\d!@#$%^&*()_+\-=]{8,20}$/,
    emailCode: /^[0-9A-Za-z]{6}$/,
}